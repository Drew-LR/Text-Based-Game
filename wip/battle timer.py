    #self.display_timer =  ['#', '#', '#', '#', '#', '#','#', '#', '#', '#']         
        
    # def countdown(self): # this function is for setting a turn timer during battles.
    #     for i in range(0, 1):
    #         t = threading.Timer(1.0, self.countdown)
    #         t.start()
    #         self.display_timer = self.display_timer[:-1]
    #         print(term.home + term.move_down(9) + term.clear_eol + "Battle Timer {}".format(''.join(self.display_timer)))
       
    #         if self.display_timer == []:
    #             t.cancel()